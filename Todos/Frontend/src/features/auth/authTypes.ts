export interface InitialStateStructure {
  userDetails: object;
  tokenDetails: string;
  isLoading: boolean;
}

export interface registerUserInterface {
  fullname: string;
  email: string;
  password: string;
  confirmPassword: string;
}
